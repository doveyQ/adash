# --- DATABASE CONFIG ---
# In Docker, 'postgres' matches the service name in docker-compose.yml
DATABASE_URL=postgresql://adash:your_secure_password@postgres:5432/adash

# --- SECURITY ---
# This key must match the one used by the Python Agent
API_KEY=generate_a_long_random_string_here